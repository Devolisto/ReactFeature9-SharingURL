import products from "../products";

const initialState = {
    products: products,
}

export const reducer = (state = initialState, action) => {
    switch (action.type) {
        default: 
        return state;
    }
}